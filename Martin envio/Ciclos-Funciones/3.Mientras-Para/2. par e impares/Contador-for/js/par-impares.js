/**
 * Funcion 
 * Autor: 
 * 
 */
function parImp(pcontar, pnumero){
    let contar = pcontar;
    let numero = pnumero;
     return contar;
    }
    const  parImpExp=function(pcontar, pnumero){
        let contar = pcontar;
        let numero = pnumero;
        return contar;
    }