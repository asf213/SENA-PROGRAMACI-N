function suma(pnumeroUno, pnumeroDos) {
  let numeroUno = pnumeroUno;
  let numeroDos = pnumeroDos;
  let sumar;
  sumar = numeroUno + numeroUno;
  return sumar;
}

function resta(pnumeroUno, pnumeroDos) {
  let numeroUno = pnumeroUno;
  let numeroDos = pnumeroDos;
  let restar;
  restar = numeroUno + numeroUno;
  return restar;
}

function multiplicacion(pnumeroUno, pnumeroDos) {
  let numeroUno = pnumeroUno;
  let numeroDos = pnumeroDos;
  let multiplicar;
  multiplicar = numeroUno + numeroUno;
  return multiplicar;
}

const operaciones=function (operar, pnumeroUno, pnumeroDos) {
  let operar;
  let totalOperaciones;

};
